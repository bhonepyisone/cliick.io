═══════════════════════════════════════════════════════════════════════════════
🔐 SECRETS PROTECTION - QUICK REFERENCE
═══════════════════════════════════════════════════════════════════════════════

✅ DO THIS:
───────────────────────────────────────────────────────────────────────────────
1. Create .env file with REAL secrets (NOT committed)
   - Store all API keys, tokens, passwords here
   - .gitignore automatically prevents commits

2. Create .env.example with PLACEHOLDER values (committed)
   - Use placeholder format: your_api_key_here
   - Developers copy this to .env and fill in real values

3. Use environment variables in code:
   const apiKey = process.env.GEMINI_API_KEY;
   const supabaseKey = process.env.SUPABASE_SERVICE_ROLE_KEY;

4. Let pre-commit hook protect you:
   - Automatically scans before every commit
   - Blocks commits with secrets
   - No action needed on your part!

───────────────────────────────────────────────────────────────────────────────

❌ DON'T DO THIS:
───────────────────────────────────────────────────────────────────────────────
1. Commit .env files with real secrets
   - Pre-commit hook will BLOCK this automatically

2. Hardcode secrets in source code
   - Pre-commit hook will detect patterns and BLOCK

3. Put real secrets in .env.example
   - Always use placeholders for example files

4. Upload credentials.json, *.key, *.pem files
   - These are blocked in .gitignore

───────────────────────────────────────────────────────────────────────────────

🚨 IF PRE-COMMIT HOOK BLOCKS YOUR COMMIT:
───────────────────────────────────────────────────────────────────────────────
Message: ❌ BLOCKED: Potential secret found in backend/server.js

Solution:
1. Remove the secret from the file
2. Move it to .env file
3. Load it via: process.env.VARIABLE_NAME
4. Try commit again

If you MUST bypass (NOT recommended):
  git commit --no-verify

═══════════════════════════════════════════════════════════════════════════════

📁 FILE STRUCTURE:
───────────────────────────────────────────────────────────────────────────────
.env                    ← REAL secrets (not committed, .gitignore blocks it)
.env.example            ← TEMPLATE with placeholders (committed)
backend/.env            ← REAL secrets for backend (not committed)
backend/.env.example    ← TEMPLATE for backend (committed)

═══════════════════════════════════════════════════════════════════════════════

🔑 SECRETS TO PROTECT:
───────────────────────────────────────────────────────────────────────────────
✓ API Keys (Gemini, AWS, Firebase, etc.)
✓ Service Role Keys (Supabase, Firebase, etc.)
✓ OAuth Secrets (Facebook, Google, Twitter, etc.)
✓ Database Passwords and Connection Strings
✓ Payment Provider Keys (Stripe, PayPal, etc.)
✓ Email Service Credentials
✓ Private Keys (.pem, .key, .jks files)
✓ JWT Secrets
✓ Webhook Secrets

═══════════════════════════════════════════════════════════════════════════════

🛠️ SETUP (Windows/PowerShell):
───────────────────────────────────────────────────────────────────────────────
# Configure Git to use hooks
git config core.hooksPath .git/hooks

# Make hook executable (PowerShell)
icacls .git\hooks\pre-commit.ps1 /grant:r "%USERNAME%:F"

Done! Hook will now run automatically before each commit.

═══════════════════════════════════════════════════════════════════════════════

📚 DETAILED GUIDE:
───────────────────────────────────────────────────────────────────────────────
See: SECRETS_PROTECTION_GUIDE.md (in project root)

═══════════════════════════════════════════════════════════════════════════════
